# VaultPilot 产品发展规划 & 竞品对比

> 生成时间: 2026-06-18
> 目标: 从"能用"到"好用"再到"离不开"

---

## 一、竞品功能对比矩阵

### 1.1 核心竞品

| 产品 | 类型 | 核心优势 | VaultPilot 对标 |
|------|------|----------|-----------------|
| **Obsidian + Copilot** | 插件生态 | 最大插件市场(2700+), 社区成熟 | ❌ 缺少插件系统 |
| **Obsidian + Smart Connections** | 语义搜索 | 本地向量嵌入, 相关笔记发现 | ⚠️ 需增强 |
| **Notion AI** | 云端协作 | 团队协作强, 数据库视图 | ❌ 非目标用户 |
| **Logseq** | 大纲式 | 块级引用, 日记流 | ⚠️ 可借鉴 |
| **Mem** | AI 原生 | 自动整理, 会议转录 | 🔵 部分对标 |
| **NotebookLM** | 研究工具 | 多源聚合, 播客生成 | 🔵 部分对标 |
| **MindWiki** | AI 原生 | 端到端 MCP 桥接 | 🔴 直接竞争 |

### 1.2 功能差距分析

#### 🔴 VaultPilot 缺失的关键功能

| 功能 | 竞品现状 | VaultPilot 现状 | 优先级 |
|------|----------|-----------------|--------|
| **向量语义搜索** | Smart Connections, Copilot 都有 | 仅有 FTS5 全文搜索 | P0 |
| **笔记链接图谱** | Obsidian 核心功能 | 无 | P1 |
| **本地 LLM 支持** | Ollama/LM Studio 集成 | 仅云端 API | P1 |
| **多模态输入** | 图片/PDF/URL 理解 | 仅图片附件 | P2 |
| **内联编辑** | Copilot Composer | 无 | P2 |
| **相关笔记推荐** | Smart View 面板 | 无 | P1 |
| **流式响应** | 所有竞品 | 需确认 | P0 |
| **会话导出** | Markdown/PDF | 无 | P2 |

#### 🟡 VaultPilot 已有但需增强

| 功能 | 当前状态 | 竞品标准 | 改进方向 |
|------|----------|----------|----------|
| 搜索 | FTS5 关键词 | 语义+关键词混合 | 添加向量搜索 |
| MCP Server | 基础工具 | 完整 CRUD + 搜索 | 工具补齐 |
| AI 对话 | 单轮问答 | 多轮上下文 + 记忆 | 上下文管理 |
| 笔记管理 | 基础 CRUD | 模板 + 批量操作 | 功能丰富 |

#### 🟢 VaultPilot 优势 (保持)

| 优势 | 说明 |
|------|------|
| **本地优先** | 数据完全在本地, 隐私安全 |
| **原生桌面** | WinUI 3 性能好, 非 Electron |
| **Rust 后端** | 内存安全, 高性能 |
| **MCP 协议** | 标准化 AI 工具接口 |

---

## 二、发展路线图

### Phase 1: 核心体验 (2-3 周)

**目标**: 解决"能用但不好用"的问题

#### 1.1 流式响应 [P0]
```
现状: 等待完整响应才显示
目标: 逐字/逐块流式显示
实现: SSE (Server-Sent Events) 或 WebSocket
测试: 
  - 单元测试: 流式解析器
  - 集成测试: 模拟流式响应
  - E2E: 真实 AI 请求流式显示
```

#### 1.2 响应取消优化 [P0]
```
现状: 取消后需等待超时
目标: 立即取消, 清除状态
实现: CancellationToken 传播优化
测试:
  - 单元测试: 取消状态机
  - 集成测试: 并发请求取消
```

#### 1.3 错误恢复 [P0]
```
现状: 网络错误后需手动重试
目标: 自动重试 + 用户提示
实现: 指数退避重试策略
测试:
  - 单元测试: 重试逻辑
  - 集成测试: 模拟网络故障
```

---

### Phase 2: 智能搜索 (3-4 周)

**目标**: 从"关键词匹配"到"语义理解"

#### 2.1 向量嵌入基础 [P0]
```
技术选型:
  - 嵌入模型: nomic-embed-text (本地) 或 text-embedding-3-small (云端)
  - 向量库: sqlite-vss 或 Qdrant (本地)
  - 存储: 与现有 SQLite 集成

实现步骤:
  1. 添加嵌入生成服务 (Rust sidecar 或 Python 脚本)
  2. 笔记索引时生成嵌入
  3. 搜索时混合 FTS5 + 向量相似度
  4. UI 显示相似度分数

测试:
  - 单元测试: 嵌入生成, 相似度计算
  - 集成测试: 索引构建, 混合搜索
  - 性能测试: 10k 笔记索引时间 < 5min
  - 准确性测试: 语义相似查询召回率 > 80%
```

#### 2.2 相关笔记推荐 [P1]
```
功能:
  - 编辑/查看笔记时, 右侧面板显示相关笔记
  - 基于向量相似度排序
  - 支持手动关联

测试:
  - 单元测试: 相关度算法
  - UI 测试: 面板显示, 点击跳转
```

#### 2.3 搜索增强 [P1]
```
功能:
  - 搜索历史记录
  - 搜索建议 (自动补全)
  - 过滤器 (标签, 日期, 类型)
  - 搜索结果高亮

测试:
  - 单元测试: 过滤逻辑, 高亮算法
  - E2E: 搜索流程完整性
```

---

### Phase 3: 本地 AI (4-5 周)

**目标**: 支持完全离线的 AI 体验

#### 3.1 Ollama 集成 [P1]
```
功能:
  - 自动检测本地 Ollama 服务
  - 模型列表获取和选择
  - 对话 + 嵌入双用途
  
实现:
  1. Ollama API 客户端 (Rust)
  2. Provider 抽象层扩展
  3. 模型管理 UI
  
测试:
  - 单元测试: API 客户端, 模型解析
  - 集成测试: 模拟 Ollama 服务
  - E2E: 真实 Ollama 对话
```

#### 3.2 多 Provider 支持 [P1]
```
支持:
  - OpenAI (现有)
  - Anthropic (现有)
  - Ollama (新增)
  - LM Studio (新增)
  - 自定义 OpenAI 兼容 (新增)

测试:
  - 单元测试: Provider 切换, 配置验证
  - 集成测试: 每个 Provider 的对话流程
```

#### 3.3 嵌入模型管理 [P2]
```
功能:
  - 嵌入模型选择 (本地/云端)
  - 批量重新嵌入
  - 嵌入质量监控

测试:
  - 单元测试: 模型切换, 批量处理
  - 性能测试: 嵌入速度对比
```

---

### Phase 4: 高级功能 (5-6 周)

**目标**: 从"工具"到"伙伴"

#### 4.1 笔记链接图谱 [P1]
```
功能:
  - 双向链接 [[note]]
  - 图谱可视化 (类似 Obsidian Graph View)
  - 链接建议

实现:
  1. Markdown 解析器提取链接
  2. 图数据结构存储
  3. D3.js 或类似库可视化
  
测试:
  - 单元测试: 链接解析, 图构建
  - UI 测试: 图谱交互
  - 性能测试: 1000 节点渲染 < 1s
```

#### 4.2 多模态理解 [P2]
```
功能:
  - PDF 解析和理解
  - URL 内容抓取
  - 图片 OCR
  - 音频转录 (Whisper)

测试:
  - 单元测试: 各格式解析器
  - 集成测试: 多模态输入处理
  - 准确性测试: OCR 准确率 > 95%
```

#### 4.3 会话管理增强 [P2]
```
功能:
  - 会话导出 (Markdown, PDF, HTML)
  - 会话标签和分类
  - 会话搜索
  - 会话模板

测试:
  - 单元测试: 导出格式, 搜索逻辑
  - E2E: 导出文件完整性
```

#### 4.4 内联编辑 [P2]
```
功能:
  - AI 建议的修改以 diff 形式显示
  - 用户可接受/拒绝单个修改
  - 修改历史可回滚

测试:
  - 单元测试: diff 算法, 状态管理
  - UI 测试: diff 显示, 交互
```

---

### Phase 5: 生态扩展 (持续)

**目标**: 建立开发者生态

#### 5.1 插件系统 [P3]
```
架构:
  - 插件 API 定义
  - 插件加载机制
  - 插件沙箱 (安全隔离)

测试:
  - 单元测试: API 规范验证
  - 安全测试: 沙箱逃逸防护
  - 兼容性测试: 插件间隔离
```

#### 5.2 MCP 工具扩展 [P2]
```
当前: 基础笔记 CRUD
目标: 完整知识管理工具集

新增工具:
  - search_notes (语义搜索)
  - get_related_notes (相关笔记)
  - create_link (创建链接)
  - get_graph (获取图谱)
  - analyze_vault (知识库分析)

测试:
  - 单元测试: 每个工具的输入输出
  - 集成测试: 工具链组合
```

---

## 三、CI/CD 增强计划

### 3.1 测试覆盖率目标

| 层级 | 当前 | 目标 | 时间 |
|------|------|------|------|
| Rust 单元测试 | ~60% | 90% | Phase 1 |
| C# 单元测试 | ~30% | 80% | Phase 2 |
| 集成测试 | 基础 | 完整 | Phase 1 |
| E2E 测试 | 无 | 核心流程 | Phase 2 |
| 性能测试 | 无 | 关键路径 | Phase 3 |

### 3.2 新增 CI 测试类型

#### 语义搜索测试
```yaml
- name: Semantic Search Tests
  run: |
    cargo test semantic_search
    cargo test vector_similarity
    cargo test hybrid_search
```

#### 本地 LLM 集成测试
```yaml
- name: Ollama Integration Tests
  services:
    ollama:
      image: ollama/ollama
  run: |
    cargo test ollama_provider
    cargo test local_embedding
```

#### 多模态测试
```yaml
- name: Multimodal Tests
  run: |
    cargo test pdf_parsing
    cargo test image_ocr
    cargo test url_extraction
```

#### 性能基准测试
```yaml
- name: Performance Benchmarks
  run: |
    cargo bench search_latency
    cargo bench embedding_speed
    cargo bench index_throughput
```

### 3.3 测试数据管理

```
test-fixtures/
├── notes/
│   ├── small/          # 100 笔记
│   ├── medium/         # 1000 笔记
│   └── large/          # 10000 笔记
├── embeddings/
│   └── pre-computed/   # 预计算嵌入
└── queries/
    ├── semantic/       # 语义测试查询
    └── keyword/        # 关键词测试查询
```

---

## 四、技术债务清理

### 4.1 代码质量

| 项目 | 状态 | 行动 |
|------|------|------|
| Rust 错误处理 | anyhow | 迁移到 thiserror + anyhow |
| C# 异步模式 | 混合 | 统一 async/await |
| 测试覆盖 | 不均 | 补充缺失测试 |
| 文档 | 不全 | API 文档自动生成 |

### 4.2 架构改进

```
当前:
  WinUI ←→ Rust Agent ←→ SQLite

目标:
  WinUI ←→ Rust Agent ←→ SQLite
                ↓
           Vector Store
                ↓
           LLM Provider (Local/Cloud)
```

---

## 五、里程碑和时间线

| 阶段 | 时间 | 核心交付 | 成功指标 |
|------|------|----------|----------|
| Phase 1 | Week 1-3 | 流式响应, 错误恢复 | 响应时间 < 100ms (首字) |
| Phase 2 | Week 4-7 | 向量搜索, 相关推荐 | 搜索召回率 > 80% |
| Phase 3 | Week 8-12 | 本地 LLM, 多 Provider | 离线可用 |
| Phase 4 | Week 13-18 | 图谱, 多模态 | 功能完整 |
| Phase 5 | 持续 | 插件生态 | 开发者参与 |

---

## 六、风险和缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 向量库性能 | 搜索慢 | 基准测试 + 优化 |
| 本地 LLM 质量 | 回答差 | 多模型对比 + 回退 |
| 插件安全 | 沙箱逃逸 | 严格权限控制 |
| 测试维护 | CI 慢 | 测试分层 + 并行 |

---

## 七、下一步行动

### 立即开始 (本周)
1. ✅ 流式响应技术调研
2. ✅ 向量嵌入库选型
3. ✅ CI 测试框架搭建

### 短期 (2 周内)
1. 流式响应 MVP
2. 语义搜索 POC
3. 测试覆盖率提升到 70%

### 中期 (1 月内)
1. 向量搜索上线
2. Ollama 集成
3. 相关笔记推荐

---

*文档由 AI 生成, 需要人工审核和调整优先级*
