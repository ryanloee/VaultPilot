<UserControl
    x:Class="VaultPilot.WinUI.Controls.VersionHistoryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Text="版本历史"
                FontSize="20"
                FontWeight="SemiBold"
                VerticalAlignment="Center" />
            <Button
                x:Name="CloseButton"
                Grid.Column="1"
                Content="关闭"
                AutomationProperties.Name="关闭版本历史" />
        </Grid>

        <!-- Main content: snapshot list + diff preview -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Snapshot list -->
            <Border
                Grid.Column="0"
                Padding="8"
                Background="{ThemeResource VaultSidebarBg}"
                CornerRadius="8"
                BorderBrush="{ThemeResource VaultBorder}"
                BorderThickness="1">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Text="快照列表"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="4,0,0,4" />

                    <ListView
                        x:Name="SnapshotList"
                        Grid.Row="1"
                        SelectionMode="Single"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        AutomationProperties.Name="版本快照列表"
                        AutomationProperties.HelpText="选择一个快照查看差异">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="4" Spacing="2">
                                    <TextBlock
                                        Text="{Binding DisplayText}"
                                        FontWeight="SemiBold"
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button
                        x:Name="RestoreButton"
                        Grid.Row="2"
                        Content="恢复到所选版本"
                        HorizontalAlignment="Stretch"
                        Margin="0,8,0,0"
                        IsEnabled="False"
                        AutomationProperties.Name="恢复到所选版本" />
                </Grid>
            </Border>

            <!-- Diff preview -->
            <Border
                Grid.Column="1"
                Padding="12"
                Background="{ThemeResource VaultSidebarBg}"
                CornerRadius="8"
                BorderBrush="{ThemeResource VaultBorder}"
                BorderThickness="1">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Text="差异对比"
                        FontSize="14"
                        FontWeight="SemiBold" />

                    <!-- Diff stats -->
                    <StackPanel
                        Grid.Row="1"
                        Orientation="Horizontal"
                        Spacing="16"
                        Margin="0,0,0,4">
                        <TextBlock x:Name="DiffAdditions" Foreground="Green" />
                        <TextBlock x:Name="DiffDeletions" Foreground="Red" />
                    </StackPanel>

                    <!-- Diff content -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="DiffContent" />
                    </ScrollViewer>

                    <!-- Empty state -->
                    <TextBlock
                        x:Name="DiffEmptyText"
                        Grid.Row="2"
                        Text="选择一个快照查看与当前版本的差异"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Visibility="Visible" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
